import React from 'react';
import Logo from '../Logo';
import { Nav } from './styled';

const Header = () => {
    return (
        <Nav>
            <Logo />
        </Nav>
    );
};

export default Header;
